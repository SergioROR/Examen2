services: 
  postgresql:
    container_name: postgresql
    image: postgres:17
    restart: always
    env_file: .env
    ports: 
      - "5432:5432"
    volumes: 
      - pgdata:/var/lib/postgresql
      - ./db/init_db.sql:/docker-entrypoint-initdb.d/initdb.sql

volumes:
  pgdata: